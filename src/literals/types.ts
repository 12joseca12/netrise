/**
 * Claves de literales. Añade aquí las claves que uses en la app
 * para tener tipado y autocompletado.
 */
export type LiteralsMap = {
  // Ejemplo: descomenta y amplía según necesites
  // "common.welcome": string;
  // "common.cta": string;
  [key: string]: string;
};

export type Locale = "es" | "en";
